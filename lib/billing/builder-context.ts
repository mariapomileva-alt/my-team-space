/** @deprecated Import from `@/lib/billing/builder-context-types` (client) or `load-builder-billing` (server). */
export type {
  BuilderBillingContext,
  BuilderLockReason,
} from "@/lib/billing/builder-context-types";
export {
  builderLockMessage,
  builderUsageLabel,
  defaultBuilderBillingContext,
} from "@/lib/billing/builder-context-types";
export { loadBuilderBillingContext } from "@/lib/billing/load-builder-billing";
